.col-md-6.col-md-offset-3
  .panel.panel-default
    .panel-heading.text-center
      Modifier les informations de mon compte
    .panel-body
      .form-horizontal
        = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
          = render 'error_messages', object: f.object, action: "mis à jour"
          .form-group
            .col-md-3
              = f.label :email, class: 'control-label'
            .col-md-9
              = f.email_field :email, autofocus: true, class: 'form-control'
          
          - if devise_mapping.confirmable? && resource.pending_reconfirmation?
            %div
              Currently waiting confirmation for: #{resource.unconfirmed_email}
          .form-group
            .col-md-3
              = f.label :password, class: 'control-label'
            .col-md-9
              = f.password_field :password, autocomplete: "off",class: 'form-control'
              %i (Laisser le champ blanc si vous ne voulez pas le changer)
          .form-group
            .col-md-3
              = f.label :password_confirmation, class: 'control-label'
            .col-md-9
              = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control'
          .form-group
            .col-md-3
              = f.label :current_password, class: 'control-label'
            .col-md-9
              = f.password_field :current_password, autocomplete: "off", class: 'form-control'
              %i (Veuillez entrer votre mot de passe pour confirmer les changements)
          = f.submit "Mettre à jour les informations", class: "btn btn-primary btn-lg btn-block"
        %br
        = link_to "Supprimer mon compte", registration_path(resource_name),  method: :delete, data: { confirm: "Êtes vous certain?" }, class: "btn btn-danger btn-lg btn-block"
