- @program.properties.each do |p|
	%p
		%strong
			= p.p_type
		= p.value
- if @program.has_courses?
	= link_to 'Courses', program_courses_path(@program)
- if @program.has_modules?
	= link_to 'Modules', program_p_modules_path(@program)
= link_to 'Back', catalog_programs_path(@catalog)