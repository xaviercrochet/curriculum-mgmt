- @course.properties.each do |p|
	%p
		%strong
			= p.p_type + ":"
		= p.value

= link_to 'Back', polymorphic_url([@context, @course])
= link_to 'Delete', @course, method: :delete, data: { confirm: 'Are you sure?'}