- @p_module.properties.each do |p|
	%p
		%strong
			= p.p_type + ":"
		= p.value

= link_to 'Sub Modules', p_module_sub_modules_path(@p_module)
= link_to 'Courses', p_module_courses_path(@p_module)
= link_to 'Back', program_p_modules_path(@program)
