%h1 Listing program modules

%table
	%tr
		%th
		%th
		%th
		%th
		%th
		%th
	- @p_modules.each do |p|
		%tr
			%td= p.name
			%td= link_to 'Show', p
			%td= link_to 'Delete', p_module_path(@catalog, p), method: :delete, data:{confirm: 'Are you sure?'}

= link_to 'Back', @catalog

