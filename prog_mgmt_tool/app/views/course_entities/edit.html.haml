%h1 Editing course entity
= form_for([@program, @course, @course_entity]) do |f|
	%p
		= f.label :professor
		= f.text_field :professor

	%p
		= f.label :credits
		= f.text_field :credits

	%p
		= select_year(Time.now)

	%p
		= f.submit
= link_to 'Back', program_course_path(@program, @course)